export class Category {
    constructor(
        public id: number,
        public icon: string,
        public name: string,
       // slider.created_date,slider.last_modified_by,slider.last_modified_date
    )
    {};
}
